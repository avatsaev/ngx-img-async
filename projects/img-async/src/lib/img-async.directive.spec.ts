import { ImgAsyncAnimDirective } from './img-async-anim.directive';

describe('ImgAsyncAnimDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgAsyncAnimDirective();
    expect(directive).toBeTruthy();
  });
});
